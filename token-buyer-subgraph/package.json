{
  "name": "token-buyer-subgraph",
  "license": "GPL-3.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goerli": "graph deploy --studio --network goerli token-buyer",
    "deploy:mainnet": "graph deploy --studio --network mainnet token-buyer",
    "create-local": "graph create --node http://localhost:8020/ token-buyer",
    "remove-local": "graph remove --node http://localhost:8020/ token-buyer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 token-buyer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
